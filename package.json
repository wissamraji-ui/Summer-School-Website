{
  "name": "summer-school-website",
  "version": "1.0.0",
  "private": true,
  "description": "Summer School on Modular Forms website",
  "scripts": {
    "dev": "echo \"Open: http://127.0.0.1:5173\" && python3 -m http.server 5173 --bind 127.0.0.1",
    "build": "vite build",
    "dev:open": "sh -c 'python3 -m http.server 5173 --bind 127.0.0.1 & pid=$!; sleep 1; open http://127.0.0.1:5173; wait $pid'",
    "dev:chrome": "sh -c 'python3 -m http.server 5173 --bind 127.0.0.1 & pid=$!; sleep 1; open -a \"Google Chrome\" http://127.0.0.1:5173; wait $pid'",
    "start": "sh -c 'PORT=${PORT:-8080}; echo \"Serving on http://0.0.0.0:$PORT\"; python3 -m http.server \"$PORT\" --bind 0.0.0.0'"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
